import { Text, View } from "react-native";

const Home = () => {
  return (<View className="flex-1 items-center justify-center">
      <Text className="text-xl font-bold">Doctor Home</Text>
    </View>);
};

export default Home;